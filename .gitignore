# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Test cache
.pytest_cache/

# Virtual environments
.venv/
venv/

# Distribution / packaging
dist/
build/
*.egg-info/
*.egg

# IDE
.idea/
.vscode/
*.swp
*.swo

# Playwright
.playwright/

# Output files (keep directory structure, ignore contents)
outputs/logs/*
!outputs/logs/.gitkeep
outputs/screenshots/*
!outputs/screenshots/.gitkeep
outputs/reports/*
!outputs/reports/.gitkeep
outputs/temp/*
!outputs/temp/.gitkeep
outputs/data/*
!outputs/data/.gitkeep
outputs/traces/*
!outputs/traces/.gitkeep
outputs/_pytest_*

frontend/node_modules/
frontend/dist/
frontend/src/components.d.ts

# Vite build 产物：由 `cd frontend && pnpm build` 生成，不入 git。
# 详见 README 「Web 控制台构建产物」节。
argus_py/api/static/assets/
argus_py/api/static/index.html

# OpenAPI intermediate build artifact（codegen 中间产物，不含 git）
openapi.json

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db
config/llm.env
config/llm.local.env
config/.fernet_key
config/browser-states/*.json
